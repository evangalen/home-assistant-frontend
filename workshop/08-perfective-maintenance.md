# Workshop: Taking back control of your code
(continues where [07-adaptive-maintenance.md](./07-adaptive-maintenance.md) file ended)

## Perfective Maintenance
Unfortunately, I did not got round to creating a workshop assignment for Perfective Maintenance 🫣

But, I do have some suggestion 😁

What about creating a custom quickfix using ast-grep. 
Creating custom quickfixes is IMO the hidden killer feature of ast-grep.
And I use it to more quickly transform code when adding new features.

You can have a look at the `rewrite-lsp-`.. rules that I secretly commit inside the `ast-grep/rules/rewrite/lsp` folder.
